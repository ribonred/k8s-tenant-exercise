[project]
name = "multi-tenant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.1.6",
    "kopf>=1.37.4",
    "kubernetes>=32.0.0",
    "psycopg-binary>=3.2.4",
    "pydantic>=2.10.6",
]
